---
import Layout from '../layouts/Layout.astro';
import InterestCTA from '../components/InterestCTA.astro';
---

<Layout title="숨이 | 종아리·발 마사지로 이완을 돕는 라이프 케어" description="잘 쉬는 몸을 만드는 시간. 숨이는 종아리·발 마사지를 통해 다리의 긴장을 풀고 이완을 돕는 라이프 이완 케어입니다.">
  <section class="hero">
    <h1>잘 쉬는 몸을 만드는 시간</h1>
    <p class="tagline">숨이는 일상 속 이완과 쉼을 기록합니다.</p>
    <p class="tagline-sub">종아리와 발 마사지를 통해, 몸이 풀리는 순간도 만들어보고 있습니다.</p>
  </section>

  <section class="intro">
    <h2>3세대를 위한 이완 케어</h2>
    <ul class="generations">
      <li><strong>여성</strong> — 하루를 마친 뒤에도 풀리지 않는 다리, 종아리부터의 이완</li>
      <li><strong>아이</strong> — 성장기 아이들의 긴장된 다리, 잘 자는 날을 위한 준비</li>
      <li><strong>부모</strong> — 아이를 돌보는 손과 몸, 먼저 챙겨야 할 나의 이완</li>
    </ul>
    <InterestCTA fromSection="intro" />
    <a href="/journal" class="link link-secondary">저널에서 모두 보기</a>
  </section>

  <div class="bottom-whitespace" aria-hidden="true"></div>
</Layout>

<style>
  .hero {
    margin-bottom: 3rem;
  }
  .hero h1 {
    font-size: 1.75rem;
    font-weight: 700;
    margin: 0 0 0.75rem;
  }
  .tagline {
    color: var(--color-muted);
    margin: 0 0 0.25rem;
    max-width: 36ch;
  }
  .tagline-sub {
    color: var(--color-muted);
    margin: 0;
    max-width: 40ch;
    font-size: 0.95rem;
  }
  .intro h2 {
    font-size: 1.15rem;
    margin: 0 0 0.75rem;
    font-weight: 600;
  }
  .generations {
    list-style: none;
    padding: 0;
    margin: 0 0 1.5rem;
    font-size: 0.9rem;
    color: var(--color-muted);
  }
  .generations li {
    padding: 0.5rem 0;
    border-bottom: 1px solid var(--color-border);
  }
  .generations strong {
    color: var(--color-text);
  }
  .link {
    color: var(--color-text);
    text-decoration: underline;
  }
  .link-secondary {
    font-size: 0.9rem;
    color: var(--color-muted);
  }
  .link-secondary:hover {
    color: var(--color-text);
  }
  .bottom-whitespace {
    min-height: 3rem;
  }
</style>

<script>
  import { getPostHogCommonProperties } from '../lib/posthog/common';

  document.querySelectorAll('[data-cta="interest"]').forEach((el) => {
    el.addEventListener('click', () => {
      const from = (el as HTMLElement).dataset.fromSection ?? 'unknown';
      try {
        (window as unknown as { posthog?: { capture: (e: string, p: Record<string, string>) => void } }).posthog?.capture('sumi_interest_cta_click', {
          ...getPostHogCommonProperties(),
          from_section: from,
        });
      } catch (_) {}
    });
  });
</script>
